apiVersion: v1
kind: Pod
metadata:
  name: myapp
spec:
  containers:
  - name: myapp
    image: myapp:1.0
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    
    ports:
      - containerPort: 3000
